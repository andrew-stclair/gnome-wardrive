#!/usr/bin/make -f

%:
	dh $@ --with python3 --buildsystem=meson

override_dh_auto_configure:
	dh_auto_configure -- --buildtype=release --prefix=/usr

override_dh_auto_test:
	# Skip tests for now as they may require special setup

override_dh_python3:
	dh_python3 --shebang=/usr/bin/python3