name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y python3-gi libgtk-4-dev libadwaita-1-dev gir1.2-gtk-4.0 \
          gir1.2-adw-1 gir1.2-nm-1.0 gir1.2-geoclue-2.0 meson ninja-build python3-requests python3-gpxpy
    
    - name: Build and test
      run: |
        meson setup builddir
        meson compile -C builddir
        python3 test-wifi-scanning.py
        python3 -c "import sys; sys.path.insert(0, 'src'); import main"

  build-deb:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install build dependencies
      run: |
        sudo apt update
        sudo apt install -y debhelper dh-python meson ninja-build libgtk-4-dev \
          libadwaita-1-dev python3-dev libgirepository1.0-dev gir1.2-gtk-4.0 gir1.2-adw-1 \
          gir1.2-nm-1.0 gir1.2-geoclue-2.0 build-essential devscripts python3-requests python3-gpxpy
    
    - name: Build Debian package
      run: |
        export DEBEMAIL="github-actions@example.com"
        export DEBFULLNAME="GitHub Actions"
        
        # Set up meson build first to ensure everything is configured
        meson setup builddir --buildtype=release
        
        # Build the Debian package using debuild (architecture-independent)
        debuild -us -uc -b
        
        # Verify the package was built and contains the application
        if [ -f ../gnome-wardrive_*.deb ]; then
          echo "✅ Package built successfully:"
          ls -la ../gnome-wardrive_*.deb
          echo "📋 Package contents:"
          dpkg-deb --contents ../gnome-wardrive_*.deb | grep -E "(gnome-wardrive|\.py)" || echo "No Python files found in package!"
          
          # Test package installation in isolated environment
          echo "🧪 Testing package installation..."
          sudo dpkg -i ../gnome-wardrive_*.deb || echo "Package installation failed, trying to fix dependencies..."
          sudo apt-get install -f -y
          
          # Test that the executable exists and can import properly
          echo "🔍 Testing executable..."
          if command -v gnome-wardrive >/dev/null 2>&1; then
            echo "✅ gnome-wardrive executable found in PATH"
            # Test imports (but don't start GUI)
            python3 -c "import sys; sys.path.insert(0, '/usr/share/gnome-wardrive'); import gnome_wardrive.main; print('✅ Application modules import successfully')" || echo "⚠️ Import test failed"
          else
            echo "❌ gnome-wardrive executable not found!"
            exit 1
          fi
        else
          echo "❌ No .deb package found!"
          exit 1
        fi
    
    - name: Upload Debian package
      uses: actions/upload-artifact@v4
      with:
        name: gnome-wardrive-deb
        path: ../*.deb
        retention-days: 30
    
