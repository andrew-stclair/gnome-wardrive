#!/usr/bin/env python3

import os
import sys
import signal
import locale
import gettext

VERSION = '@VERSION@'
pkgdatadir = '@pkgdatadir@'
localedir = '@localedir@'

sys.path.insert(1, pkgdatadir)
signal.signal(signal.SIGINT, signal.SIG_DFL)

locale.bindtextdomain('gnome-wardrive', localedir)
locale.textdomain('gnome-wardrive')
gettext.install('gnome-wardrive', localedir)

if __name__ == '__main__':
    import gi

    from gi.repository import Gio
    resource = Gio.Resource.load(os.path.join(pkgdatadir, 'gnome-wardrive.gresource'))
    resource._register()

    from gnome_wardrive.main import main
    sys.exit(main())